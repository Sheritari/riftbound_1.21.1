package com.riftbound.loot;

/**
 * Item categories for affix pools and drop rules. Add new categories as equipment types are introduced.
 */
public enum ItemLootCategory {
    ONE_HAND_SWORD,
    /** Helmets with armour as primary stat (stub — no helmet item yet). */
    HELMET_ARMOUR
}
